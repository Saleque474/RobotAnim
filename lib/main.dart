import 'package:flutter/material.dart';

void main() {
  runApp(RobotAnimApp());
}

class RobotAnimApp extends StatelessWidget {
  const RobotAnimApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
